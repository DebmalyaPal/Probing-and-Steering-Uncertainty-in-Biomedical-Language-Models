# ============================================================================
# Probing and Steering Uncertainty in Biomedical Language Models
# Direct dependencies only.  For exact reproducibility use requirements-freeze.txt.
#
# Install:
#   pip install -r requirements.txt
#
# For 7B models (BioMistral, Meditron) also install:
#   pip install bitsandbytes>=0.41.0
# ============================================================================

# --- Core ML -----------------------------------------------------------------
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.27.0
tokenizers>=0.19.0
safetensors>=0.4.0

# --- Quantization (required for BioMistral-7B and Meditron-7B) --------------
# Uncomment if you intend to run 7B models:
# bitsandbytes>=0.41.0

# --- Scientific computing ----------------------------------------------------
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0

# --- HuggingFace ecosystem ---------------------------------------------------
huggingface_hub>=0.20.0
datasets>=2.14.0

# --- Biomedical NLP (original paper) ----------------------------------------
sacremoses>=0.0.53
spacy>=3.7.0
scispacy>=0.5.4
negspacy>=1.0.4

# --- Evaluation metrics ------------------------------------------------------
bert-score>=0.3.13
nltk>=3.8.0

# --- Visualisation -----------------------------------------------------------
matplotlib>=3.7.0

# --- Data handling -----------------------------------------------------------
pandas>=2.0.0
tqdm>=4.65.0
networkx>=3.0

# --- Jupyter / Notebooks -----------------------------------------------------
jupyter>=1.0.0
jupyterlab>=4.0.0
nbformat>=5.9.0          # notebook generation (scripts/generate_notebooks.py)
ipywidgets>=8.0.0
ipykernel>=6.0.0

# --- Parsing / utilities -----------------------------------------------------
pysbd>=0.3.4             # sentence boundary detection
lark>=1.1.0              # grammar parsing
pyarrow>=12.0.0          # fast columnar I/O for datasets
